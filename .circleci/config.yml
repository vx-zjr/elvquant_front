version: 2.1

jobs:
  qa:
    docker:
      - image: cimg/python:3.12-node
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-front-pip-{{ checksum "pyproject.toml" }}
            - v1-front-npm-{{ checksum "package-lock.json" }}
      - run:
          name: Install development dependencies
          command: |
            python -m venv .venv
            . .venv/bin/activate
            python -m pip install --upgrade pip
            python -m pip install -e ".[dev]"
            npm ci
      - save_cache:
          key: v1-front-pip-{{ checksum "pyproject.toml" }}
          paths:
            - ~/.cache/pip
      - save_cache:
          key: v1-front-npm-{{ checksum "package-lock.json" }}
          paths:
            - ~/.npm
      - run:
          name: Run tests
          command: |
            . .venv/bin/activate
            pytest
      - run:
          name: Run lint
          command: |
            . .venv/bin/activate
            ruff check
      - run:
          name: Build React cockpit
          command: npm run build

workflows:
  qa:
    jobs:
      - qa
